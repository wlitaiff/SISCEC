CLOSE ALL
USE CRECEB NEW
USE CECCREC	NEW
SELECT("CRECEB")
DO WHILE !EOF()
	SELECT("CECCREC")
	APPEN BLAN
	CECCREC->CODCLIR:=(CRECEB->FORNECEDOR)
	CECCREC->DOCDUPR:=(CRECEB->DUPLICATA)
	CECCREC->DTEMISR:=(CRECEB->EMISSAO)
	CECCREC->DTVENCR:=(CRECEB->VENCIMENTO)
	CECCREC->VLDOCTR:=(CRECEB->VALOR)
	CECCREC->SLDOCTR:=(CRECEB->DEBITO)
	SELECT("CRECEB")
	DBSKIP()
ENDDO
CLOSE ALL